# Page Topology

1. Cloned GSAP toggle hero — full viewport, default ON
2. Existing TSUKUMO99 tour content
3. Existing footer, cheer widget, and ticket modal

The previously added List/Grid showcase is no longer rendered. The original project navigation is preserved for downstream content but stays hidden until the first viewport has been passed.